defmodule DoctorsApi.RoomChannelTest do
  use Phoenix.ChannelTest
  use DoctorsApi.ConnCase

  alias DoctorsApi.RoomChannel

  test "joining" do
#     {:ok, _, socket} =
#       socket("user:id", %{some_assigns: 1})
#       |> subscribe_and_join(RoomChannel, "room:lobby", %{"id" => 3})
#     IO.inspect(socket)
  end
end
